import React from 'react';
import { Button } from '../ui/button';

const LogoutButton = () => {
    return (
        <div>
            <Button>Logout</Button>
        </div>
    );
};

export default LogoutButton;